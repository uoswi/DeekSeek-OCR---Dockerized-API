# Requirements for DeepSeek_OCR_pdf_to_md.py
# Install with: pip install -r requirements_pdf_to_md.txt

# PDF Processing
PyMuPDF>=1.23.0

# Image Processing
Pillow>=10.0.0

# Deep Learning
torch>=2.0.0

# DeepSeek OCR Dependencies (if running locally, not via API)
# Uncomment these if you have the DeepSeek OCR model installed locally:
# vllm>=0.5.0
# transformers>=4.40.0
# flash-attn>=2.5.0

# Note: If you're using the script to call a remote API (localhost:8000),
# you only need PyMuPDF, Pillow, and torch
